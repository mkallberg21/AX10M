{
  "name": "@ax10m/backtest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist *.tsbuildinfo out",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "backtest": "tsc -b && node ./dist/cli.js"
  },
  "dependencies": {
    "@ax10m/attribution": "workspace:*",
    "@ax10m/canonical": "workspace:*",
    "@ax10m/recovery-engine": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^6.0.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.0"
  }
}
