/**
 * SamCart adapter — ADVISORY (measurement + attribution). See ./adapter.ts.
 *
 * Exports the adapter class, its config interface, and the verify / decline-map helpers.
 */
export * from './adapter.js';
