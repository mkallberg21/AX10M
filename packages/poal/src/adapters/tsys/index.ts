/**
 * TSYS / Transaction Express adapter — DRIVE-mode sale against a stored token,
 * with fail-closed ingress and reconciliation handled out-of-band. See ./adapter.ts.
 */
export * from './adapter.js';
export * from './decline-map.js';
